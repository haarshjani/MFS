{
  "name": "portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:ui-kit": "pnpm --filter ui-kit run build",
    "start:main": "pnpm --filter main start",
    "start:accounts": "pnpm --filter accounts start",
    "start:transections": "pnpm --filter transections start",
    "start:ui-kit" :"pnpm --filter ui-kit watch",
    "start":"run-p start:ui-kit start:main start:accounts start:transections"

  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
